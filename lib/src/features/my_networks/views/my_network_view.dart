import 'package:flutter/material.dart';

class MyNetworkView extends StatelessWidget {
  const MyNetworkView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      heightFactor: 40,
      child: Text('Network Screen Pending'),
    );
  }
}
